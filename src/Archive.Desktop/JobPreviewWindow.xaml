<Window x:Class="Archive.Desktop.JobPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview Operations"
        Icon="Assets/icon.ico"
        Height="360"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Preview Operations"
                   FontSize="20"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,0"
                   Text="This is a dry run. No job settings or files were changed."
                   TextWrapping="Wrap" />

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Files to add" />
            <TextBlock Grid.Column="1" x:Name="FilesToAddTextBlock" />

            <TextBlock Grid.Row="1" Text="Files to update" />
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="FilesToUpdateTextBlock" />

            <TextBlock Grid.Row="2" Text="Files to delete" />
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="FilesToDeleteTextBlock" />

            <TextBlock Grid.Row="3" Text="Files unchanged" />
            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="FilesUnchangedTextBlock" />

            <TextBlock Grid.Row="4" Text="Files skipped" />
            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="FilesSkippedTextBlock" />

            <TextBlock Grid.Row="5" Text="Estimated bytes to transfer" />
            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="BytesTextBlock" />
        </Grid>

        <StackPanel Grid.Row="3"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Close"
                    Width="88"
                    Click="CloseButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
